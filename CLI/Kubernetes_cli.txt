minikube start
minikube dashboard
    minikube dashboard --url --port=5555
    kubectl proxy --address='0.0.0.0' --disable-filter=true
#kind: 
#    namespaces
#    pods
#    deployments
#    services
#    configmaps
#    pv
#    pvc

kubectl get <kind>
kubectl delete <kind> <kind-name>
kubectl logs <kind-name>



kubectl config get-contexts // check current used namespace
kubectl config set-context --current --namespace=<namespace-name> //switch to namespace

kubectl apply -f <file/to/*.yaml>  //create your kind
kubectl port-forward svc/<service> <service-port>:<desired-port>
kubectl delete replicaset $(kubectl get replicaset -o jsonpath='{ .items[?(@.spec.replicas==0)].metadata.name }') // delete unused replicasets

kubectl create secret docker-registry my-registry-secret \
  --docker-server=registry.example.com \
  --docker-username=my-username \
  --docker-password=my-password \
  --docker-email=my-email@example.com

  
  
kubectl get nodes -o jsonpath='{.items[*].spec.podCIDR}' // get CIDR
